# install/

## Useful information (humans)

Platform install guides linked from the Downloads page:

| Page | Purpose |
|------|---------|
| `chrome.html` | Download ZIP + load unpacked Chrome extension |
| `macos.html` | macOS desktop app status + account waitlist CTA |

## Useful information (AI)

- Chrome artifact: `website/downloads/veritasvpn-chrome.zip` (rebuild from `clients/browser-extension` after changes).
- When Chrome Web Store URL exists, replace the ZIP flow with `https://chrome.google.com/webstore/detail/...`.
- When macOS DMG exists, set the primary CTA on `macos.html` to the release asset URL.
