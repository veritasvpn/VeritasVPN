[Unit]
Description=Prepare VeritasVPN backup metrics directory
After=local-fs.target
Before=k3s.service

[Service]
Type=oneshot
ExecStart=/usr/bin/install -d -o root -g root -m 0755 /var/lib/veritasvpn/metrics
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
