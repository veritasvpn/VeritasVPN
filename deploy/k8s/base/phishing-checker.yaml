apiVersion: v1
kind: Service
metadata:
  name: phishing-checker
  namespace: veritas
  labels:
    app: phishing-checker
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
  selector:
    app: phishing-checker
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: phishing-checker
  namespace: veritas
  labels:
    app: phishing-checker
spec:
  replicas: 1
  revisionHistoryLimit: 2
  selector:
    matchLabels:
      app: phishing-checker
  template:
    metadata:
      labels:
        app: phishing-checker
    spec:
      automountServiceAccountToken: false
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: phishing-checker
          image: localhost:31500/phishing-checker@sha256:e479551cee67afd54068e0fb5b453c6933fd24de5a6b5aed30bbcade91481465
          imagePullPolicy: IfNotPresent
          env:
            - name: RATE_LIMIT_PER_MINUTE
              value: "10"
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 3
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            requests:
              cpu: 25m
              memory: 32Mi
            limits:
              cpu: 150m
              memory: 128Mi
          securityContext:
            runAsNonRoot: true
            runAsUser: 65532
            runAsGroup: 65532
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
